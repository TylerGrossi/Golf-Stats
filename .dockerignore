# Only golf_mcp.py, paths.py, requirements.txt and data/ are copied into the image;
# this keeps the build context small so `fly deploy` does not upload the .pbix files.
.venv/
venv/
env/
__pycache__/
*.py[cod]

archive/
dashboards/
docs/
notebooks/
.ipynb_checkpoints/

.git/
.gitignore
.claude/
.mcp.json
*.md

~$*.xlsx
~$*.xlsm
.DS_Store
Thumbs.db
